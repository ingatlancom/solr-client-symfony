grumphp:
    git_hook_variables:
        EXEC_GRUMPHP_COMMAND: 'docker-compose run --rm --no-deps php8'
    ascii: ~
    tasks:
        ecs: ~
        psalm:
            ignore_patterns:
                - 'tests/'
        phpstan: ~
        phpunit:
            always_execute: true
        composer: ~
