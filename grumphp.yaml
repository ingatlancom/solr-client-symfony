parameters:
    git_hook_variables:
        EXEC_GRUMPHP_COMMAND: 'docker-compose run --rm --no-deps php'
    ascii: ~
    tasks:
        ecs: ~
        psalm:
            ignore_patterns:
                - 'tests/'
        phpstan: ~
        phpunit:
            always_execute: true
        composer: ~
